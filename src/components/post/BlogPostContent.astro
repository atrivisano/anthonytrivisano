---
export interface Props {
    class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class={`prose prose-neutral lg:prose-lg dark:prose-invert max-w-none ${className}`}>
    <slot />
</div>