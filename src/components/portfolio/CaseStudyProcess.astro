---
import Container from '../common/Container.astro';

interface ProcessPhase {
    title: string;
    description: string;
}

interface Props {
    phases?: ProcessPhase[];
}

const { phases = [] } = Astro.props;
---
<section class="py-16 sm:py-24">
    <Container>
        <h2 class="text-3xl font-bold tracking-tight text-white mb-8">Development Process</h2>

        <slot />

        {phases && phases.length > 0 && (
            <div class="mt-10 space-y-12">
                {phases.map((phase, index) => (
                    <div class="border-l-4 hover:border-l-8 border-white pl-6 transition-all">
                        <h3 class="text-xl font-bold text-white">{phase.title}</h3>
                        <p class="mt-2 text-neutral-300">{phase.description}</p>
                    </div>
                ))}
            </div>
        )}
    </Container>
</section>